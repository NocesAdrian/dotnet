// See https://aka.ms/new-console-template for more information
using System.Linq;
int[] arr = {1,4,5,10,2,0};
Console.WriteLine(arr.Min()); // output the largest number on the array
Console.WriteLine(arr.Max()); // output the lowest number on the array
Console.WriteLine(arr.Sum()); // ouput the sum of all numbers on the array
